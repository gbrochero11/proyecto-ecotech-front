import { FilerthorecaPipe } from './filerthoreca.pipe';

describe('FilerthorecaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilerthorecaPipe();
    expect(pipe).toBeTruthy();
  });
});
