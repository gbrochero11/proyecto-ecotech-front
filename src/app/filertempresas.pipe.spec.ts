import { FilertempresasPipe } from './filertempresas.pipe';

describe('FilertempresasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilertempresasPipe();
    expect(pipe).toBeTruthy();
  });
});
